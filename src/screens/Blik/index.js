import {Text, View} from "react-native";

const BlikScreen = ({ navigation }) => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>BlikScreen</Text>
        </View>
    );
}

export default BlikScreen;
